# Phase 12 evidence

- The Phase 10 PWA is the single React application wrapped by Capacitor 8.
- Android and iOS native projects are generated and synchronized.
- `restock://approval` returns to a resumable server-side workflow.
- Native sessions use iOS Keychain / Android Keystore-backed AES-GCM storage.
- Push permission, registration, receipt, and action listeners are wired.
- Android debug APK and unsigned iOS Simulator builds pass locally.
- Physical-device notification/deep-link tests and store enrollment remain explicit
  human/external gates; no enrollment fee was paid.
