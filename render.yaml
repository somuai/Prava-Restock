services:
  - type: web
    name: restock-api
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /ready
    autoDeployTrigger: checksPass
    envVars:
      - key: RESTOCK_ENV
        value: production
      - key: RESTOCK_DEMO_MODE
        value: "0"
